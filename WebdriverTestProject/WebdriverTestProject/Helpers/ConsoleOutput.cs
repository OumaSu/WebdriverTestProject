using System;

namespace WebdriverTestProject.Helpers
{
    public static class ConsoleOutput
    {
        public static void WriteConsole(string outputString) => Console.WriteLine(outputString);
    }
}